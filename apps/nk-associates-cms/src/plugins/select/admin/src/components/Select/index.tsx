import React from "react";

import {
  HeaderLayout,
  Typography,
  Layout,
  GridLayout,
  Box,
  Grid,
  GridItem,
} from "@strapi/design-system";

const Select = (e) => {
  console.log("select plugin", e);
  return <div>Select</div>;
};

export default Select;
